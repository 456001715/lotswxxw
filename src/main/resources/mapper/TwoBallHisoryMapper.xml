<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 双色球开奖历史(two_ball_hisory) -->
<mapper namespace="com.lots.lotswxxw.dao.TwoBallHisoryMapper">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="twoBallHisoryMap" type="com.lots.lotswxxw.domain.po.TwoBallHisoryPo">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="red_number" property="redNumber" jdbcType="VARCHAR" />
        <result column="blue_number" property="blueNumber" jdbcType="VARCHAR" />
        <result column="creat_time" property="creatTime" jdbcType="TIMESTAMP" />
    </resultMap>
    <!-- This code was generated by TableGo tools, mark 1 end. -->
    
    <!-- This code was generated by TableGo tools, mark 2 begin. -->
    <!-- 表查询字段 -->
    <sql id="allColumns">
        tbh.id, tbh.red_number, tbh.blue_number, tbh.creat_time
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->
    
    <!-- 查询所有数据 -->
    <select id="findTwoBallHisoryAll" resultMap="twoBallHisoryMap">
        SELECT
        <include refid="allColumns" />
        FROM two_ball_hisory tbh
    </select>
    
    <!-- 根据条件参数查询数据列表 -->
    <select id="findTwoBallHisoryByCondition" resultMap="twoBallHisoryMap" parameterType="map">
        SELECT
        <include refid="allColumns" />
        FROM two_ball_hisory tbh WHERE 1 = 1
        <if test="redNumber != null and redNumber != ''">
            AND tbh.red_number LIKE CONCAT('%', #{redNumber}, '%')
        </if>
        <if test="blueNumber != null and blueNumber != ''">
            AND tbh.blue_number LIKE CONCAT('%', #{blueNumber}, '%')
        </if>
        <if test="creatTime != null">
            AND tbh.creat_time = #{creatTime}
        </if>
    </select>
    
    <!-- 根据主键查询数据 -->
    <select id="findTwoBallHisoryByIds" resultMap="twoBallHisoryMap" parameterType="list">
        SELECT
        <include refid="allColumns" />
        FROM two_ball_hisory tbh WHERE tbh.id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
    
    <!-- 插入数据 -->
    <insert id="insertTwoBallHisory" parameterType="map">
        INSERT INTO two_ball_hisory (
             red_number, blue_number, creat_time
        ) VALUES (
            #{redNumber},
            #{blueNumber},
            #{creatTime}
        )
    </insert>
    
</mapper>